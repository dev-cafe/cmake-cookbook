appveyor-vs:
  definitions:
    - Boost_FORCE_SUPERBUILD: 'ON'

appveyor-msys:
  definitions:
    - Boost_FORCE_SUPERBUILD: 'ON'

travis-linux:
  definitions:
    - Boost_FORCE_SUPERBUILD: 'ON'

travis-osx:
  definitions:
    - Boost_FORCE_SUPERBUILD: 'ON'

local:
  definitions:
    - Boost_FORCE_SUPERBUILD: 'ON'
  env:
    - VERBOSE_OUTPUT: 'ON'
