appveyor-vs:
  failing_generators:
    - 'Visual Studio 15 2017 Win64'

appveyor-msys:
  failing_generators:
    - 'MSYS Makefiles'
    - 'Ninja'

# Fails to generate imported target for UUID
circle-pgi:
  skip_generators:
    - 'Unix Makefiles'
    - 'Ninja'
