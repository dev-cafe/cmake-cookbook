appveyor:
  definitions:
    - USE_LIBRARY: 'ON'

drone:
  definitions:
    - USE_LIBRARY: 'ON'

travis-linux:
  definitions:
    - USE_LIBRARY: 'ON'

travis-osx:
  definitions:
    - USE_LIBRARY: 'ON'

local:
  definitions:
    - USE_LIBRARY: 'ON'
