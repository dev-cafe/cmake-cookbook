appveyor:
  definitions:
    - USE_LIBRARY: 'ON'

drone:
  definitions:
    - CMAKE_CXX_COMPILER: 'pgc++'
    - USE_LIBRARY: 'ON'

travis-linux:
  definitions:
    - USE_LIBRARY: 'ON'

travis-osx:
  definitions:
    - USE_LIBRARY: 'ON'

local:
  definitions:
    - USE_LIBRARY: 'ON'
