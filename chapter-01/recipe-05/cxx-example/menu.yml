appveyor-vs:
  definitions:
    - USE_LIBRARY: 'ON'

appveyor-msys:
  definitions:
    - USE_LIBRARY: 'ON'

circle-intel:
  definitions:
    - CMAKE_CXX_COMPILER: 'icpc'
    - USE_LIBRARY: 'ON'

circle-pgi:
  definitions:
    - CMAKE_CXX_COMPILER: 'pgc++'
    - USE_LIBRARY: 'ON'
      
travis-linux:
  definitions:
    - USE_LIBRARY: 'ON'

travis-osx:
  definitions:
    - USE_LIBRARY: 'ON'

local:
  definitions:
    - USE_LIBRARY: 'ON'
