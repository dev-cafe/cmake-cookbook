appveyor-vs:
  definitions:
    - CMAKE_BUILD_TYPE: 'Release'
    - CMAKE_CONFIGURATION_TYPES: 'Release'

appveyor-msys:
  definitions:
    - CMAKE_BUILD_TYPE: 'Release'

circle-intel:
  definitions:
    - CMAKE_BUILD_TYPE: 'Release'

circle-pgi:
  definitions:
    - CMAKE_BUILD_TYPE: 'Release'

travis-linux:
  definitions:
    - CMAKE_BUILD_TYPE: 'Release'

travis-osx:
  definitions:
    - CMAKE_BUILD_TYPE: 'Release'

local:
  definitions:
    - CMAKE_BUILD_TYPE: 'Release'
